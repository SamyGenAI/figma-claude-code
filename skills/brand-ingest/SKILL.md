---
description: Extract brand design tokens from a Figma file and generate a theme. Provide a Figma URL, or answer a few color questions if you don't have one.
---

Use the **brand-ingest** agent to set up a new brand theme.

The user's input (if any): "$ARGUMENTS"

If the user provided a Figma URL or brand colors in their input, pass that directly to the agent. Otherwise, the agent will ask for what it needs.
